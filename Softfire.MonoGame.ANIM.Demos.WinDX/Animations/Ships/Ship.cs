using Microsoft.Xna.Framework;

namespace Softfire.MonoGame.ANIM.Demos.WinDX.Animations.Ships
{
    public class Ship : Animation
    {
        public Ship(Animation parent, int id, string name, string texturePath, Vector2 position) : base(parent, id, name, texturePath, position)
        {

        }
    }
}